name: deploy backend
on:
  push:
    branches: [ main ]
jobs:
  deploy:
    runs-on: self-hosted
    steps:
    - name: atualizar código
      run: |
        cd ~/p2-sam-backend
        git fetch origin main
        git reset --hard origin/main
        npm install
        pm2 restart backend